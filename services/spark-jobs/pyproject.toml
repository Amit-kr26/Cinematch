[project]
name = "spark-jobs"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "pyspark==3.5.1",
    "redis>=5.0",
    "psycopg2-binary>=2.9",
    "numpy>=1.26",
    "kafka-python-ng>=2.2",
    "mlflow>=2.13",
]

[tool.setuptools]
py-modules = ["streaming", "bootstrap"]
