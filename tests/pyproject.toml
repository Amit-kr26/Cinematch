[project]
name = "tests"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "pytest>=8.0",
    "pytest-asyncio>=0.23",
    "httpx>=0.27",
    "fakeredis>=2.23",
    "spark-jobs",
    "api",
    "event-simulator",
]

[tool.uv.sources]
spark-jobs = { workspace = true }
api = { workspace = true }
event-simulator = { workspace = true }
