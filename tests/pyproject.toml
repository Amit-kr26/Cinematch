[project]
name = "tests"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "pytest>=9.0.3",
    "pytest-asyncio>=1.3.0",
    "httpx>=0.28.1",
    "fakeredis>=2.35.1",
    "spark-jobs",
    "api",
    "event-simulator",
]

[tool.uv.sources]
spark-jobs = { workspace = true }
api = { workspace = true }
event-simulator = { workspace = true }
