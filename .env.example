# CineMatch Lite runtime configuration (all optional)
PORT=7860        # HTTP port; HuggingFace Spaces injects its own
DB_PATH=/data/app.db
DATA_DIR=/data   # only needed when retraining instead of using the seed
